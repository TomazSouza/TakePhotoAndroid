package com.example.takephotoandroid.provider;

import androidx.core.content.FileProvider;

public class ImageCaptureProvider extends FileProvider {
    @Override
    public boolean onCreate() {
        return super.onCreate();
    }
}
